let numeroInicial = -0;
let numeroFim = -25;

if (numeroInicial < 0 || numeroFim < 0) {
  console.log("nao podemos verificar os dados do numero");
} else {
  for (i = numeroInicial; i <= numeroFim; i++) {
    console.log(i);
  }
}
